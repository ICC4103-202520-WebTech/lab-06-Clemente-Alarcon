<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Recipes" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Your app styles (includes everything in app/assets/stylesheets) -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Bootstrap CSS (for styling) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">

    <!-- Trix CSS (required for ActionText editor to be visible/clickable) -->
    <link rel="stylesheet" href="https://unpkg.com/trix@2.0.0/dist/trix.css">

    <%= yield :head %>

    <!-- Importmap JS bundle (loads Turbo, Trix, ActionText, etc.) -->
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Simple Navbar -->
    <nav class="navbar navbar-dark bg-dark mb-4">
      <div class="container">
        <a class="navbar-brand" href="/">RecipeBook</a>
        <div class="d-flex gap-2">
          <%= link_to "All Recipes", recipes_path, class: "btn btn-outline-light btn-sm" %>
          <%= link_to "New Recipe", new_recipe_path, class: "btn btn-success btn-sm" %>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if notice %>
        <div class="alert alert-success"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger"><%= alert %></div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
